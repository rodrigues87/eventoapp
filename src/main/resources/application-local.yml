spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: 'postgres'
    password: '123456'
    url: 'jdbc:postgresql://localhost:5432/eventosApp'
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect